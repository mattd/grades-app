import React from 'react';
import IconButton from 'material-ui/IconButton';
import MoreVertIcon from 'material-ui-icons/MoreVert';

const TermDetailMenu = () => {
    return (
        <IconButton>
            <MoreVertIcon />
        </IconButton>
    );
};

export default TermDetailMenu;