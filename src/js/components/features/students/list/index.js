import React from 'react';

import { Feature } from 'components/containers';

const StudentListFeature = () => {
    return (
        <Feature title="Students" />
    );
};

export default StudentListFeature;