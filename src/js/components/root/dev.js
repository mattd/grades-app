import React from 'react';

import App from '../app';
import DevTools from '../dev-tools';

const Root = () => {
    return (
        <div>
            <App />
            <DevTools />
        </div>
    );
};

export default Root;