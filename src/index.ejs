<!doctype html>
<html lang="en-us">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible" content="ie=edge">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1 maximum-scale=1">

        <title>
            Grades App
            <% if (!FIREBASE_ENV) { %>
                <%= '- Development' %>
            <% } else if (FIREBASE_ENV != 'production') { %>
                <%= '- ' + FIREBASE_ENV.charAt(0).toUpperCase() + FIREBASE_ENV.slice(1) %>
            <% } %>
        </title>
    </head>
    <body>
        <div id="root"></div>
        <script src="/static/bundle.js?c=<%= BUILD_COMMIT %>"></script>
    </body>
</html>